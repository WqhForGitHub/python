# 61. 词法分析器（Lexer）

通用规则驱动的词法分析器，可独立使用或被其他工具复用。

## 特性
- 标识符 / 关键字（可自定义集合）
- 整数（含十六进制 0x..）/ 浮点数（含科学计数法）
- 字符串 / 字符（支持转义）
- 单行 `//` / 多行 `/* */` 注释
- 多字符 / 单字符运算符
- 行列号记录、详细错误提示

## 用法
```
python lexer.py samples/demo.src
```
