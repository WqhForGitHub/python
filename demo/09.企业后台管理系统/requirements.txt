# 企业后台管理系统依赖
# 使用 PEP 621 [project] 表格式

[project]
name = "enterprise-admin"
version = "0.1.0"
description = "企业后台管理系统 - Django Demo"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "Django>=5.0",
    "Pillow>=10.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
